# WebSocket Chat

WebSocket-based chat demo using Hibana's WebSocket handler.

## Running

```bash
mix deps.get && mix run
```

Server starts on **port 4003**.

## Endpoints

```bash
# Home page
curl http://localhost:4003/
```

## WebSocket

Connect to `ws://localhost:4003/chat` to join the chat room.
